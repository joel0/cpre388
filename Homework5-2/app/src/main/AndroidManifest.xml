<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="cpre388.jmay.homework5_2">

    <permission
        android:name="cpre388.jmay.homework5musicplayer"
        android:protectionLevel="signature" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StatusActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <service
            android:name=".PlayerService"
            android:exported="true">

            <intent-filter>
                <action android:name="cpre388.jmay.homework5_2.action.PLAY"/>
                <action android:name="cpre388.jmay.homework5_2.action.PAUSE"/>
                <action android:name="cpre388.jmay.homework5_2.action.STOP"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </service>
    </application>

</manifest>